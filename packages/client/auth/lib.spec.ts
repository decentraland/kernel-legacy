import 'mocha'

describe('auth system', () => {
  it('allows a user to request an email verification code sent', () => {
  })

  it('allows a user to login with an email and a code', () => {
  })

  it('may reject the code if its invalid', () => {
  })

  it('allows the user to log out', () => {
  })
})