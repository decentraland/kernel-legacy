describe('ProtocolConnection', () => {
  it('Closes when underlying connection closes', () => {})
  it('Emits Profile Topis', () => {
    // expect(profile) to have shape: { userId , alias }
  })
  it('Emits Chat', () => {
    // expect(chat message) to have shape: { from, to, metadata, body }
  })
  it('Emits Position messageas', () => {
    // expect(posiiton) to have shape: number[7]
  })
  it('Emits Ping messageas', () => {
    // expect(posiiton) to have shape: number[7]
  })
})
